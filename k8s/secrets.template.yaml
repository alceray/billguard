# DO NOT COMMIT actual values. This is a template.
# In dev: fill in manually and apply with kubectl.
# In prod: generated by External Secrets Operator from AWS Secrets Manager.
apiVersion: v1
kind: Secret
metadata:
  name: billguard-secrets
  namespace: billguard-dev
  labels:
    app.kubernetes.io/name: billguard
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  DATABASE_PASSWORD: "REPLACE_ME"
  DATABASE_URL: "postgresql://billguard:REPLACE_ME@postgres-service:5432/billguard"
  AUTH0_DOMAIN: "REPLACE_ME"
  AUTH0_AUDIENCE: "REPLACE_ME"
  # Phase 2+
  PLAID_CLIENT_ID: ""
  PLAID_SECRET: ""
  OPENAI_API_KEY: ""
